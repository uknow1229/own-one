<%= form_with model: [post_blog, blog_comment] do |f| %>
  <%= f.text_area :comment, rows: 5, placeholder: "コメントをここに" %>
  <%= button_tag type: "submit", class: "btn btn-success ms-3" do %>
    <i class="fas fa-paper-plane"></i>
  <% end %>
<% end %>