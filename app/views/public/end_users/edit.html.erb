<div class="user-profile-edit container mt-4">
  <div class="row justify-content-center">
    <div class="col-sm-8">

        <h1 class="text-center">Edit Profile</h1>
        <%= render "public/shared/error_messages", resource: @end_user %>

        <%= form_with model: @end_user, url:end_user_path do |f| %>
          <div class="user-profile-edit__details form-group mb-4">
            <label>画像</label>
            <%= f.file_field :profile_image, accept: "image/*", class: 'form-control mb-2' %>

            <label>ユーザー名</label>
            <%= f.text_field :user_name, class: 'form-control mb-2' %>

            <label>身長</label>
            <%= f.number_field :height, class: 'form-control mb-2', in: 1..250 %>

            <label>体重</label>
            <%= f.number_field :body_weight, class: 'form-control mb-2', step: "0.1", in: 1..150, value: number_with_precision(@end_user.body_weight, precision: 1) %>

            <label>年齢</label>
            <%= f.number_field :age, class: 'form-control mb-2', in: 1..150 %>

            <label>性別</label>
            <%= f.select :sex, options_for_select(EndUser.sexes.map { |k, _v| [t("activerecord.attributes.end_user.sexes.#{k}"), k] }),
            { data: {} } ,class: 'form-control mb-2' %>

            <label>目標体重</label>
            <%= f.number_field :target_weight, class: 'form-control mb-2', step: "0.1", in: 1..150 %>

            <label>目標カロリー</label>
            <%= f.number_field :target_calorie, class: 'form-control mb-2', in: 1..10000 %>

            <label>活動レベル</label>
            <%= f.select :activelevel, options_for_select(EndUser.activelevels.map { |k, _v| [t("activerecord.attributes.end_user.activelevels.#{k}"), k] }),
            { data: {} } ,class: 'form-control mb-2' %>

            <table class="table">
              <p>※活動レベルは以下の表を目安に設定してください</p>
              <thead>
                <tr>
                  <th class="col-md-2">活動レベル</th>
                  <th class="col-md-6">目安</th>
                </tr>
              </thead>
              <tbody>
                <tr>
                  <th>Level 1</th>
                  <td>運動はしない：生活の大部分が座位で、静的な活動が中心の場合</td>
                </tr>
                <tr>
                  <th>Level 2</th>
                  <td>軽く運動する：職場内での移動や立位での作業や、軽いスポーツ、家事等のいずれかを含む場合</td>
                </tr>
                <tr>
                  <th>Level 3</th>
                  <td>活発に活動：移動や立位の多い仕事への従事者、あるいは、活発な運動習慣をもっている場合</td>
                </tr>
              </tbody>
            </table>

            <label>自己紹介</label>
            <%= f.text_field :introduction, class: 'form-control mb-2' %>

            <label>E-Mail</label>
            <%= f.text_field :email, class: 'form-control mb-2' %>

            <div class="user-profile-edit__btn text-center">
              <%= f.submit '更新する', :class => "btn btn-success mt-4" %>
            </div>
          </div>
        <% end %>

    </div>
  </div>
</div>


